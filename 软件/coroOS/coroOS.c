#include "coroOS.h"
/*
int thread_sleep(struct pt *pt)
{
    static int i,sleepable= 1;
    PT_BEGIN();
    for(i= 0;i < ;i++)
    {
        if(sleepdisable[i]== 1) sleepable= 0;
    }
    if(sleepable)
    {
        if(sleepwhenready== 0) __WFI();
        else
        {
            OS_SHUTDOWN;
            __WFI();
            OS_RESUME;
        }
    }
}
*/
